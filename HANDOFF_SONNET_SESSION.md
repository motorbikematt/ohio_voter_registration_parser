# Handoff: Ohio Voter Dashboard City Data Debugging

**Date**: 2026-05-09 10:45 UTC  
**Status**: In Progress — Blocked on Windows Python execution  
**Owner**: Motorbike (motorbikematt@gmail.com)

---

## Problem Statement

The Ohio voter registration dashboard is not rendering city-level data. Users can view **counties and precincts** with full 6-chart format (party affiliation, decade distribution, party×decade stacked bar, party×generation stacked bar, UNC shadow, cohort counts), but **cities/townships are missing or showing wrong format**.

### Root Cause (Confirmed)

1. **Script exists but never ran**: `jurisdictional_groupings.py` (~800 lines) was written to generate individual chart JSON files for cities, but was **never executed**.
2. **Pipeline menu broken**: `ohio_voter_pipeline.py` option 4 ("Generate jurisdictional groupings") had **no handler in `_dispatch()` function**.
3. **Wrong data in manifest**: `manifest.json` references `{county_slug}_city_summary.json` files (type="table") generated by `voter_data_cleaner_v2.py`, NOT the 6-chart format that `jurisdictional_groupings.py` generates.

### What's Been Fixed

- ✅ Added handler for option 4 in `ohio_voter_pipeline.py` (lines 461–465):
  ```python
  elif choice == "4":
      import jurisdictional_groupings as jg
      logger, _ = jg.setup_logger()
      logger.info("Running jurisdictional groupings for cities...")
      jg.main(jurisdictions_to_process=['cities'], output_format='json', logger=logger)
  ```
- ✅ Validated syntax with `python3 -c "import ast; ast.parse(...)"` — no errors.
- ✅ Confirmed `jurisdictional_groupings.py` has correct logic:
  - Lines 228–423: `aggregate_jurisdiction()` — computes party affiliation, decade/generation distributions, UNC shadow for a single jurisdiction.
  - Lines 426–454: `export_jurisdiction_json()` — writes 6 individual JSON files per jurisdiction.
  - Lines 457–570: `main()` — loads enriched Parquet, spawns ThreadPoolExecutor tasks, exports all jurisdictions.

---

## Current Blocker

**Cannot run `jurisdictional_groupings.py` from Linux bash** because:
- The venv (`C:\Users\motorbikematt\.venv\Scripts\activate`) is Windows-native.
- Bash bash environment has system Python3 with **no polars, xlsxwriter, psutil, orjson**.
- Polars is required in `jurisdictional_groupings.py` line ~50 for DataFrame operations.

**Next Step**: Execute the script in **Windows PowerShell with venv activated** to generate city-level chart files.

---

## Files to Know

### Core Data
- **Source**: `D:\vibe\election-data (1)\source\SWVF_*.txt` (4 split SWVF files, 7.9M voters)
- **Enriched cache**: `D:\vibe\election-data (1)\voter_data_enriched_v2.parquet` (partitioned by county)
  - Contains 135 columns including 15 cohort columns + all registration/election history.
  - Pre-classified with `COHORT_FAMILY` (PURE_R, UNC_LAPSED_R, MIXED_ACTIVE, MIXED_LAPSED, UNC_NO_PRIMARY, UNC_LAPSED_D, PURE_D).

### Scripts
- **`ohio_voter_pipeline.py`** (~460 lines)
  - Menu driver. Option 4 now routes to `jurisdictional_groupings.main()`.
  - Lines 453–459 (old): _dispatch() had no handler for choice "4" — **FIXED**.

- **`jurisdictional_groupings.py`** (~800 lines) — **CRITICAL PATH**
  - Lines 106–146: Cohort/color/generation config (reused from voter_data_cleaner_v2).
  - Lines 228–423: `aggregate_jurisdiction(enriched_df, jurisdiction_key, jurisdiction_type)` — filters voters, computes all 6 chart datasets.
  - Lines 426–454: `export_jurisdiction_json(jurisdiction_type, aggregated_data)` — writes JSON.
  - Lines 457–570: `main(jurisdictions_to_process=['cities'], output_format='json', logger=logger)` — orchestrates.
  - **Expected output**: `docs/data/{county_slug}_city_party_affiliation.json`, `{county_slug}_city_decade_distribution.json`, etc. (6 files per jurisdiction type).

- **`voter_data_cleaner_v2.py`** (~3,650 lines)
  - Current source of summary table JSON (type="table") — these are **not** the 6-chart format.
  - Lines ~1500+: `export_json()` + chart export functions.

### Dashboard
- **`docs/charts.js`** — Manifest-driven chart renderer.
  - Expects `sections[i].charts` array with objects like:
    ```json
    {
      "title": "Party Affiliation",
      "type": "doughnut",
      "dataUrl": "data/{slug}_city_party_affiliation.json"
    }
    ```
  - Current city sections have **one** entry pointing to `data/{slug}_city_summary.json` (table format) — **wrong**.

- **`manifest.json`** — 533 county sections (✅ correct), 89 city sections (❌ wrong format).
  - Example current city entry:
    ```json
    {
      "name": "Montgomery",
      "scope": "city",
      "charts": [
        {
          "title": "City Summary",
          "type": "table",
          "dataUrl": "data/montgomery_city_summary.json"
        }
      ]
    }
    ```
  - **Needs**: 6 chart objects like counties have.

---

## Exact Next Steps

### Step 1: Generate City Chart JSON Files (Windows Python)

```powershell
cd D:\vibe\election-data (1)
.\.venv\Scripts\activate
python ohio_voter_pipeline.py
# Select option 4: "Generate jurisdictional groupings (cities, townships, etc.)"
```

**Expected output** in `docs/data/`:
- `{county_slug}_city_party_affiliation.json` (doughnut data)
- `{county_slug}_city_decade_distribution.json` (bar data)
- `{county_slug}_city_party_by_decade.json` (stacked bar data)
- `{county_slug}_city_party_by_generation.json` (stacked bar data)
- `{county_slug}_city_unc_shadow.json` (doughnut data)
- `{county_slug}_city_cohort_counts.json` (summary counts)

For all 89 Ohio counties: adams_city_*.json, allen_city_*.json, ... wyandot_city_*.json.

### Step 2: Update manifest.json

Replace city section chart definitions with 6-chart format. Example pattern:

```json
{
  "name": "Montgomery",
  "scope": "city",
  "charts": [
    {
      "title": "Party Affiliation",
      "type": "doughnut",
      "dataUrl": "data/montgomery_city_party_affiliation.json"
    },
    {
      "title": "Age by Decade",
      "type": "bar",
      "dataUrl": "data/montgomery_city_decade_distribution.json"
    },
    {
      "title": "Party × Decade",
      "type": "bar",
      "dataUrl": "data/montgomery_city_party_by_decade.json"
    },
    {
      "title": "Party × Generation",
      "type": "bar",
      "dataUrl": "data/montgomery_city_party_by_generation.json"
    },
    {
      "title": "UNC Shadow",
      "type": "doughnut",
      "dataUrl": "data/montgomery_city_unc_shadow.json"
    },
    {
      "title": "Cohort Summary",
      "type": "table",
      "dataUrl": "data/montgomery_city_cohort_counts.json"
    }
  ]
}
```

Apply this pattern to all 89 city sections (one per Ohio county).

### Step 3: Commit & Push to GitHub

```powershell
cd D:\vibe\election-data (1)
git add docs/data/*_city_*.json manifest.json
git commit -m "Generate city-level chart data and update manifest (cities now render 6-chart format)"
git push origin main
```

GitHub Pages rebuild: ~40 seconds. Then test the dashboard.

### Step 4: Verify Dashboard

- Open https://motorbikematt.github.io/ohio_voter_registration_parser/
- Select **City** from the Scope dropdown.
- Verify cities appear in the Geo filter.
- Click a city (e.g., Columbus) → verify 6 charts render (party affiliation doughnut, decade bar, etc.).
- Spot-check cohort counts match what you'd expect from the enriched data.

---

## Key Code References

### Cohort Taxonomy (Source of Truth)
Located in `voter_data_cleaner_v2.py` lines ~106–146:
```python
COHORT_SLICES = [
    ("PURE_R", "#E81B23"),
    ("UNC_LAPSED_R", "#F26522"),
    ("MIXED_ACTIVE", "#FFCC00"),
    ("MIXED_LAPSED", "#C6D96F"),
    ("UNC_NO_PRIMARY", "#95C11F"),
    ("UNC_LAPSED_D", "#0015BC"),
    ("PURE_D", "#0066CC"),
]
COHORT_STACK_MAP = {
    "r_pure": "PURE_R",
    "unc_r": "UNC_LAPSED_R",
    "unc_mid": "MIXED_ACTIVE",
    "unc_d": "MIXED_LAPSED",
    "d_unc": "UNC_NO_PRIMARY",
    # ... (incomplete mapping example; full version in actual file)
}
GENERATION_RANGES = {
    "Silent": (1928, 1945),
    "Boomer": (1946, 1964),
    "Gen X": (1965, 1980),
    "Millennial": (1981, 1996),
    "Gen Z": (1997, 2012),
}
```

### JSON Output Structure
Each city chart JSON follows this pattern (e.g., `montgomery_city_party_affiliation.json`):
```json
{
  "geography": "city",
  "county": "Montgomery",
  "jurisdiction": "Columbus",
  "data": {
    "labels": ["PURE_R", "UNC_LAPSED_R", "MIXED_ACTIVE", ...],
    "values": [12345, 6789, 4321, ...],
    "colors": ["#E81B23", "#F26522", "#FFCC00", ...]
  },
  "timestamp": "2026-05-09T10:45:00Z"
}
```

---

## Environment Details

- **OS**: Windows 11
- **Python venv**: `C:\Users\motorbikematt\.venv\Scripts\activate`
- **Key packages**: polars, xlsxwriter, openpyxl, psutil, orjson
- **Data directory**: `D:\vibe\election-data (1)`
- **Dashboard**: GitHub Pages, `/docs` on main branch
- **GitHub repo**: https://github.com/motorbikematt/ohio_voter_registration_parser

---

## Log References

- **Pipeline log** (latest): `D:\vibe\election-data (1)\logs\voter_analysis_20260509_104511_pipeline.log`
  - Shows: "download_manifest.json not found — falling back to today's date"
  - Indicates script was attempting to load manifest but source file missing (expected behavior).

---

## Known Issues & Gotchas

1. **File editing protocol**: For files > 150 lines, use Python patch scripts, not the Edit tool. Edit tool truncates silently at ~950 lines.
2. **Git index.lock**: If git fails, delete `D:\vibe\election-data (1)\.git\index.lock` and retry from PowerShell.
3. **ThreadPoolExecutor**: Uses 8 workers, shared memory (no IPC). Peak RSS ~45 GB during Excel export.
4. **Parquet schema**: Enriched file has 135 columns; never load raw SWVF files into Excel.

---

## Success Criteria

- [ ] Step 1: Run jurisdictional_groupings.py → city JSON files generated in `docs/data/`
- [ ] Step 2: manifest.json updated with 6-chart format for all 89 city sections
- [ ] Step 3: Changes committed & pushed to GitHub
- [ ] Step 4: Dashboard City scope renders charts for ≥3 representative cities (e.g., Columbus, Cincinnati, Cleveland)
- [ ] Spot check: Cohort counts + party distribution match expected ranges for sampled cities

---

## Questions for Sonnet Session

If anything is unclear:
1. Can you access Windows PowerShell with venv activated?
2. Are all required packages (polars, xlsxwriter, psutil, orjson) installed in the venv?
3. Does the enriched Parquet file exist at the expected path?
4. After Step 1, verify at least one city JSON file was generated — compare structure to a county JSON file.

Good luck! This is a straightforward 4-step fix once the script runs.
